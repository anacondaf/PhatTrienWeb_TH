var MakeMultiFilter = (array = []) => {
  let filterCriteria = null;

  //arrayFilterer
  return (...args) => {
    //currentArray is mutable
    let currentArray = [];

    currentArray.forEach((elem) => param1(elem));
  };
};

const param1 = (elem) => {
  return true;
};
